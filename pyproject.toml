[project]
name = "codex2lark"
version = "0.1.0"
description = "Harness-centered Feishu multi-agent runtime for ChatGPT and Codex"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
dependencies = [
    "cryptography>=45,<47",
    "lark-channel-sdk==1.0.0",
    "mcp>=1.0,<2",
    "pydantic>=2.10,<3",
]

[project.scripts]
codex2lark = "codex2lark.cli:main"

[dependency-groups]
dev = [
    "mypy>=1.15,<2",
    "pytest>=8.3,<9",
    "pytest-asyncio>=0.25,<1",
    "ruff>=0.11,<1",
]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/codex2lark"]

[tool.pytest.ini_options]
addopts = "-q"
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["codex2lark"]

[tool.uv]
package = true
